interface:
  display_name: "Ultra Simplify"
  short_description: "Simplify code with parallel cleanup agents"
  default_prompt: "Use $ultrasimplify to review this code with parallel cleanup agents, apply approved behavior-preserving simplifications, then verify the result."

policy:
  allow_implicit_invocation: false
