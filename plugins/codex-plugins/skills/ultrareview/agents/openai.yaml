interface:
  display_name: "Ultra Review"
  short_description: "Parallel deep code review"
  default_prompt: "Use $ultrareview to review the current changes with parallel review agents and return numbered prioritized issues."

policy:
  allow_implicit_invocation: false
